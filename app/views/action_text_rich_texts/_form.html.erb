<%= form_with(model: action_text_rich_text, local: true) do |form| %>
  <% if action_text_rich_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(action_text_rich_text.errors.count, "error") %> prohibited this action_text_rich_text from being saved:</h2>

      <ul>
        <% action_text_rich_text.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :record_type %>
    <%= form.text_field :record_type %>
  </div>

  <div class="field">
    <%= form.label :record_id %>
    <%= form.text_field :record_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
